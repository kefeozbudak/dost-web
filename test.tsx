const renderContent = () => {
  const block = { type: 'test' };
  switch (block.type) {
    case 'primary_school_bento':
      return (
        <section>
        </section>
      );
    case 'menu_hero':
      return (
        <section>
        </section>
      );
  }
};
