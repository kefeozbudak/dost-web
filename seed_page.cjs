const admin = require('firebase-admin');
// Just manually tell user to create the page in Pages Center, or create a quick JS script if needed.
// Actually, it's easier to just create a default export in defaultData.ts and we're good.
