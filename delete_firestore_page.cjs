const admin = require('firebase-admin');

// We don't have the service account key easily here, but we can just use a web script if needed or we could skip this as deleting from default layout often suffices if we just reset or they just requested to remove it from "the system".
